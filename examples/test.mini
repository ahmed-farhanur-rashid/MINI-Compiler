number x = 5;
number z = 0;
number y;
number age;

string str = "My name is ";
string name;

fetch(age);
fetch(name);

string output = str + name;
display(output);

repeat(x) {
    z = z + x;
    fetch(y);
    display(y);
}
